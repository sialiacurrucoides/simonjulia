---
import Tag from "./Tag.astro";

const { tags } = Astro.props;
---
    <div class="tags">
        {tags.map((tag: string) => (
        <Tag tag={tag} />
        ))}
    </div>

<style>
  .tags {
    display: flex;
    flex-wrap: wrap;
  }
</style>